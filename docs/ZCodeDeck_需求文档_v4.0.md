---
name: zcodedeck
id: zcodedeck
version: PRD-v4.0
date: 2026-09-03
status: v0.2.3 已上线(核心能力全部实弹验证)
repo: D:\ZCode\ZCodeDeck
entry: 启动悬浮窗.bat 或 py -3 zcode_deck.py
port: 17654
test: py -3 tests/test_roundtrip.py
docs: docs/ZCodeDeck_需求文档_v4.0.md(权威源);archive/ 存档旧版
---

# ZCodeDeck 需求文档

## 1. 一句话定义

Windows 桌面悬浮小窗:监控 ZCode 会话状态、远程批准或拒绝操作、续跑任务、展示套餐额度与消耗速率。软件版的 Codex Macro Pad。

解决三个问题:ZCode 在后台干活时状态不可见;权限确认必须在 ZCode 窗口内操作;coding plan 消耗没有常驻视图。

## 2. 用户与场景

单用户本机使用(作者本人),Python 3.14 + PySide6,单实例常驻托盘。

| 场景 | 触发 | 预期 |
|---|---|---|
| S1 后台监督 | ZCode 会话运行中,用户在做别的事 | 悬浮窗状态色 + 会话行实时反映思考/执行/出错,无需切窗口 |
| S2 远程审批 | 会话弹出权限确认 | 悬浮窗出卡片显示工具与命令摘要,点批准或拒绝,会话继续,全程不切窗口 |
| S3 额度感知 | 任何时候瞟一眼 | 5 小时窗与每周窗剩余量进度条;每周窗带速率刻度线与超前/落后判定 |

## 3. 功能需求

状态图例:✅ 已实弹验证(真实引擎/真实点击)| 🔧 已实现(离屏断言+回归)| 📋 规划

| ID | 需求 | 状态 | 验收判据(可执行) |
|---|---|---|---|
| FR-01 | 状态监控:执行=品牌蓝、思考=青(呼吸)、完成=绿(专属)、待批=琥珀、出错=红、空闲=灰;窗框/圆点/托盘/会话行同步 | ✅ | `curl :17654/events` 可见引擎亲发事件 |
| FR-02 | 远程审批:批准/拒绝/接受计划,显示工具名与命令预览 | ✅ | 引擎日志出现 `Allowed by PermissionRequest hook` |
| FR-03 | 审批超时回退:5 分钟无操作静默退出,ZCode 回退自身 UI 确认;卡片带倒计时,超时后留 12 秒痕迹行 | ✅ | 回路测试场景三;用户实测未卡会话;离屏断言倒计时与痕迹 |
| FR-04 | 续跑键:任务停止后 8 秒窗口内一键继续(托盘开关,默认关);紧凑两行条(上行任务标题按宽省略,下行语义行"⟳ 本轮已结束"+倒计时,右侧续跑按钮;语义行独占一行不截断)(引擎不区分完成与停下,措辞不暗示异常),平滑高度动画 | ✅ | CLI 实弹:模型续答明确回应续跑指令;离屏断言两条移除路径均复原高度 |
| FR-05 | 只读自动放行:Read/Grep/Glob/LS/TodoRead/WebSearch 免确认(托盘开关,默认关) | 🔧 | 回路测试场景六/七 |
| FR-06 | 套餐额度:5 小时窗(蓝)与每周窗(绿)剩余进度条,右侧百分比数字(不带前缀),悬停显总量/已用/重置时间 | 🔧 | 离屏断言两行进度条与配色 |
| FR-07 | 消耗速率:每周窗按已过时间算应用用量,以刻度线表达(填充越过刻度即超前),不加文字判定;完整口径悬停可见 | 🔧 | 离屏断言 marker 存在且无 verdict 文字 |
| FR-08 | 今日用量:并入标题行(今日 X · N 次),近5小时/近7天明细悬停可见,不做占比条(无天然基准) | 🔧 | 离屏断言标题行文案 |
| FR-09 | 会话模块:状态展示的唯一文字载体,每个活跃会话一行,五列固定对齐(色点|标题96px定宽省略|轮次40px|状态弹性列|时间右对齐),标题与状态文字均按像素省略,任何长度下轮次与状态严格垂直对齐,最多 4 行;纯空闲会话不占行(点开查看≠运行,SessionStart/resume 不产生空闲行),全部空闲时仅显示一行"空闲";轮次取 turn_usage 已完结计数:进行中显示已完结+1(首轮即第1轮),完成态显示已完结数;标题取 session.title,首次见到会话与每轮结束时刷新;Stop 事件可能先于该轮落账,Stop 后 4 秒延迟二读 + 每 60 秒周期补读自愈账实漂移 | 🔧 | 离屏断言:三个 SessionStart 只出一行空闲;活跃会话正常占行 |
| FR-10 | 窗口形态:宽度固定,卡片出现增高、移除后收回紧凑高度 | 🔧 | 离屏断言 163→275→163 |
| FR-11 | 位置记忆 + 开机自启(HKCU Run 键,托盘开关) | 🔧 | 重启后位置保持;注册表键存在 |
| FR-12 | 调试端点:/events 最近事件、/resolve 远程决定 | 🔧 | `curl :17654/events?count=5` |
| FR-13 | 5 小时窗速率指示(同 FR-07 逻辑) | 📋 | — |
| FR-16 | 收起为边缘状态点:30px 圆钉贴右缘,颜色实时跟随状态机;单击展开(滑出+淡入动画,垂直对齐圆钉位置并整体夹回屏内,杜绝出屏),可沿边缘拖动;收起态来审批卡时圆钉琥珀脉冲提醒(不自动展开,何时处理由用户决定);状态持久化 | 🔧 | 离屏断言三轮收起/展开循环与脉冲提醒 |
| FR-14 | 审批规则自定义(按工具/命令模式配置白名单) | 📋 | — |
| FR-15 | 会话行点击跳转对应 ZCode 会话 | 📋 | — |
| FR-23 | 合盖不休眠:看板标题行「合盖」药丸开关 + 托盘镜像项;开启=当前电源计划合盖动作改 0(先备份 AC/DC 原值),关闭/退出自动恢复;powercfg 输出按 GBK 解码,索引取匹配行末两位 | 🔧 | 打桩单测(备份/置0/恢复/默认)+本机实弹(1,1)备份恢复一致 |
| FR-22 | 品牌字形:边缘圆钉与托盘图标绘制白色 Z 字形(从本地官方 icon.png 抠取亮度为 alpha,缺资产回退几何 Z),状态色圆底保留 | 🔧 | 大尺寸渲染 ASCII 字形断言 + 圆钉白像素计数 |
| FR-21 | 发布与首次体验:首次启动 API Key 引导对话框(含隐私声明,可跳过);托盘「API Key 设置」入口;额度未设置提示可点击直达;make_release.py 一键打包 zip(源码+启动器+使用说明含隐私声明与卸载方法) | 🔧 | key 配置两态断言;zip 清单与说明内容断言;发布包 34KB/11 文件 |
| FR-20 | 今日日报长图:标题行蓝色药丸"日报"按钮(可点击感),一键生成 900px 暗色长图——主标题"今日AI牛马战报"、品牌蓝主数字、统计药丸、任务排行(彩色名次徽章+主题提炼(项目级截断优先:已知项目名开头直接收敛为项目名,任务细节如忌讳号规则不上图;别名归一宽上限;通用压缩窄上限:去动词前后缀+句中"时"分句删除+空格切词)+占比条)、今日主力调侃一句话(按占比与轮数分档拼装)、右上角日期含生成时间、底部署名"ZCodeDeck · Alex打工人万事屋 / 我自己做的小工具";保存至 reports/ 并自动打开 | 🔧 | 提炼四真实案例断言;真数据渲染+像素采样(蓝主数字/药丸/徽章/署名) |
| FR-17 | 未读完成态:会话完成后保持"已完成"(加粗),点击该行即标记已读(行消失);该会话再次完成重新进入未读;不自动回落空闲。查看会话(ZCode 内切换)不产生引擎事件,故已读手势在悬浮窗内完成 | 🔧 | 离屏断言:点击行消失、再完成重现 |
| FR-19 | 事实源回填与纠偏:每 60 秒对账——近 5 分钟工具调用输出(exec mtime)与近 15 分钟完结轮按时间戳裁决(工具输出晚于完结=执行中,反之已完成)补齐漏掉的会话;对已跟踪会话,事实时间新于我方最后事件时采纳纠正(含不受窗口限制的完结核对,纠丢 Stop 的陈旧执行/思考);行内时间用事实时间 | 🔧 | 离屏断言:时间戳裁决两向、陈旧思考/执行纠正、新轮思考不误伤 |
| FR-18 | 活跃会话存活窗:思考/执行/出错态容忍长静默(12 小时兜底防僵尸,覆盖渲染/过夜任务;曾因 4 分钟与 30 分钟两度误清长任务),空闲态 4 分钟,未读完成不按时限清除;行内相对时间提示陈旧度 | 🔧 | 离屏断言:静默 3 小时执行态保留且聚合 running、13 小时清除 |

## 4. 非功能需求

| ID | 需求 | 验收判据 |
|---|---|---|
| NFR-01 | 绝不阻塞会话:桥接脚本任何异常静默退出 0;服务不在时 3 秒超时让行 | 回路测试;手工 `echo '{}' \| py -3 deck/hook_bridge.py status Stop` 退出码 0 |
| NFR-02 | 本地优先:用量数据只读本地 sqlite;API key 只存 `~/.zcode/deck/config.json`,不入库不外发(官方配额端点除外) | 文件检查 |
| NFR-03 | 降级:无托盘/无屏环境照常启动;额度接口失败显示错误不崩 | 离屏(QT_QPA_PLATFORM=offscreen)全量断言通过 |
| NFR-04 | 性能:status/pretooluse hook ≤3 秒返回;UI 轮询 200ms | 引擎日志 hook 时长 |
| NFR-05 | 可回归:`py -3 tests/test_roundtrip.py` 七场景退出码 0(跑前须退出悬浮窗) | 命令执行 |

## 5. 接口契约

Hook 输入(stdin JSON):`session_id` `hook_event_name` `tool_name` `tool_input` `permission_mode` `transcript_path`。

Hook 输出(stdout JSON,zod 严格校验,多键即败):

```
PermissionRequest 批准  {"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}
PermissionRequest 拒绝  同上,decision={"behavior":"deny","message":"原因"}
PreToolUse 放行         {"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow","permissionDecisionReason":"..."}}
Stop 续跑               {"decision":"block","reason":"继续指令"}(顶层键,最多三次)
无输出 + exit 0         不做决定,回退 ZCode 默认行为
```

HTTP(127.0.0.1:17654):`GET /health` `GET /events?count=N` `POST /event` `POST /permission` `POST /stop-continue` `POST /pretooluse` `POST /resolve{kind,decision,pid?}`。

## 6. 运维速查

| 操作 | 命令 |
|---|---|
| 启动 | 双击 `启动悬浮窗.bat`(已在运行时自动跳过;纯 ASCII,bat 内严禁中文) |
| 退出 | 托盘右键 → 退出 |
| 卸载 hook | `py -3 deck/install_hooks.py --remove` |
| 回归测试 | `py -3 tests/test_roundtrip.py`(先退悬浮窗) |
| 错误日志 | `logs/deck_stderr.log` |
| 升级后确认 | `curl :17654/health` 看 version 字段 |

已知运维事实:用户级 hook 随引擎进程启动加载,ZCode 桌面版升级 hook 后需完整重启一次;项目级"始终允许"规则会跳过审批卡(正确语义);跑测试前须退出悬浮窗(SO_REUSEADDR 流量劫持)。

## 7. 边界与不做

不做手机端与多用户;不改 ZCode 本体(只用官方 hook/数据库/接口);不推算每日额度上限(官方无此数据,不造基准线);悬浮窗不做输入式交互(保持一瞥即得)。

## 8. 路线图候选

v0.3:审批规则自定义(FR-14)、会话点击跳转(FR-15)、5 小时窗速率(FR-13)、历史用量留存与日视图。

## 9. 风险与依赖

| 风险 | 影响 | 对冲 |
|---|---|---|
| ZCode 版本升级改动 hook 协议 | 审批/续跑失效 | 协议实锤记录在 AGENTS.md,升级后跑回路测试即可定位 |
| 官方配额接口变更或 key 失效 | 额度面板不可用 | 优雅降级为错误文案,其余功能不受影响;key 可随时更换 |
| 端口 17654 被占 | 双实例流量错乱 | /health 看 version 确认;测试自带守卫 |

## 10. 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-09-02 | 初版;对应软件 v0.2.3,核心能力(FR-01~04)全部实弹验证 |
| v1.1 | 2026-09-02 | 按用户反馈精简:百分比去"剩"前缀;速率仅保留刻度线去掉文字判定;删除今日占比条(无天然基准,易误解) |
| v1.2 | 2026-09-02 | 状态展示统一:删除整体状态文字行,会话模块为唯一文字载体,消除多会话时的重复展示 |
| v1.3 | 2026-09-02 | 审批体验修正(源于置顶任务丢卡反馈):等待 165s→300s 且卡片显示倒计时,超时留痕;AskUserQuestion 不再出卡(其实际交互在任务窗口提问框);hook 超时预算升至 330s |
| v1.4 | 2026-09-03 | 新增 FR-16 边缘状态点收起/展开(方案比选:半隐藏滑出/侧边拉手/纯托盘/状态点,取状态点,单击展开+审批自动展开);软件版本 v0.3.0 |
| v1.5 | 2026-09-03 | 按用户反馈修订:收起时审批改为圆钉脉冲提醒而非自动展开(避免打扰);新增 FR-17 未读完成态(取消 5 秒自动回落);软件版本 v0.3.1 |
| v1.6 | 2026-09-03 | FR-09 升级:会话行展示任务标题与对话轮次(本地库直读);软件版本 v0.3.2 |
| v1.7 | 2026-09-03 | 空闲会话不占行(查看≠运行),全空闲只显一行;今日用量并入标题行求紧凑;软件版本 v0.3.3 |
| — | 2026-09-03 | 文档整理:需求文档只保留最高版于 docs/(权威源)与 v1.0 于 archive/(存档起点),中间版本删除(决策已浓缩于变更记录);运行日志移至 logs/ |
| — | 2026-09-03 | 修复启动器:bat 原为 UTF-8 中文,cmd 按 GBK 解码撕裂成乱码命令导致双击失败;重写为纯 ASCII 并加"已在运行"守卫(健康检查),防止双实例 |
| — | 2026-09-03 | 修复展开出屏:collapse 动画位置被误当恢复目标,每循环右漂 90px;改为记住收起前位置,展开对齐圆钉垂直位置并夹紧屏内;软件版本 v0.3.4 |
| — | 2026-09-03 | 会话行改五列固定对齐布局,修标题长短导致的错位(根因:状态列 sizeHint 超预算时 Qt 布局会挤压前列,重列宽预算并对状态文字像素省略);软件版本 v0.3.5 |
| v2.0 | 2026-09-03 | 高度稳定性包:修续跑卡移除后高度不回落(根因:同 tick 内 sizeHint 陈旧缓存致 resize 空操作,修法 invalidate+activate 后取值);所有高度变化 140ms 平滑动画;续跑卡单行紧凑条;软件版本 v0.3.6 |
| v2.1 | 2026-09-03 | FR-17 增加点击已读手势:未读完成行点击即清除,再次完成重新未读;软件版本 v0.3.7 |
| v2.2 | 2026-09-03 | 修双任务只显示一个:活跃态 4 分钟无事件误清改为 30 分钟存活窗(长工具期间引擎静默是常态);软件版本 v0.3.8 |
| v2.3 | 2026-09-03 | 修首轮轮次为空:turn_usage 只在轮末落账,进行中轮次 = 已完结 + 1(首轮即第1轮),完成态恰为已完结数;软件版本 v0.3.9 |
| v2.4 | 2026-09-03 | 修轮次滞后一轮:Stop 时刻读账早于该轮落账,Stop 后 4 秒延迟二读 + 60 秒周期补读;软件版本 v0.3.10 |
| v2.5 | 2026-09-03 | 修任务运行中整体状态显灰:聚合状态由"最近会话"改为"严重性优先级最值"(查看产生的新空闲不再掩盖运行中任务);软件版本 v0.3.11 |
| v2.6 | 2026-09-03 | 活跃存活窗 30 分钟→12 小时:数小时级长任务(渲染/过夜)再次被误清;陈旧度由时间戳表达,上限仅为防僵尸兜底;软件版本 v0.3.12 |
| v2.7 | 2026-09-03 | 新增 FR-19 事实源回填:修"超长任务在场时新任务不显示"(根因:悬浮窗随迭代多次重启,内存态清零后无事实源回填,长任务靠事件洪流回场而安静新任务永缺);软件版本 v0.3.13 |
| v2.8 | 2026-09-03 | 修完成后仍显执行/思考:回填判定改时间戳裁决(工具输出 vs 完结谁晚),并对已跟踪活跃会话做不受窗口限制的完结核对(MBA 完成显执行中、大屏抽奖完成显思考中两案例);软件版本 v0.3.14 |
| v2.9 | 2026-09-03 | 停止条改中性文案"本轮已结束"并绑定任务标题(完成与停下在引擎侧同为轮次结束,"任务已停止"对完成任务有误导);软件版本 v0.3.15 |
| v3.0 | 2026-09-03 | 新增 FR-20 今日日报长图(分享感设计,自制工具署名);软件版本 v0.4.0 |
| v3.1 | 2026-09-03 | 修停止条截断(单行总宽超限硬裁,"本轮已结束"被拦腰):改两行布局,语义行独占一行;软件版本 v0.4.1 |
| v3.2 | 2026-09-03 | 日报传播感改造(用户六条反馈):入口药丸按钮、标题别名提炼(天地人看板手机端等)、署名 Alex打工人万事屋、删无意义行+时间并入日期、今日主力调侃一句话、蓝色主数字/统计药丸/名次徽章/副标题口吻;软件版本 v0.4.2 |
| v3.3 | 2026-09-03 | 文案精修:主标题改"今日AI牛马战报",删副标题,榜单标题去"打",署名区删小字行改为灰色斜体小字单行;软件版本 v0.4.3 |
| v3.4 | 2026-09-03 | 布局与提炼重构:游标式排版修今日主力越界重叠;三个一级模块统一"蓝色短竖标+30px加粗"标题;未命中别名的标题走窄上限通用压缩(八案例断言含防误伤);软件版本 v0.4.4 |
| v3.5 | 2026-09-03 | 分享脱敏:项目级截断规则(大屏抽奖等已知项目名开头即收敛为项目名),忌讳号等任务细节不上图;软件版本 v0.4.5 |
| v3.6 | 2026-09-03 | 修无任务仍显绿:聚合把已读完成也算进完成档,全部读完后边框挂绿;聚合改只统计可见会话(与列表同口径),完成态边框调暗与执行态区分;软件版本 v0.4.6 |
| v3.7 | 2026-09-03 | 发布包(用户推广):首次 API Key 引导+隐私声明、托盘设置入口、make_release.py 发布 zip、公众号文章 v1.0;软件版本 v0.5.0 |
| v3.8 | 2026-09-04 | 新增 FR-22 品牌字形:圆钉与托盘画官方 Z(本地资源抠取,非网页扒取);发布包含资产;配图3 重出;软件版本 v0.5.1 |
| v3.9 | 2026-09-04 | 状态色语义重排(用户点破):执行中绿改品牌蓝(蓝=正在干活),思考改青,绿色专属已完成,消除绿的双义;软件版本 v0.5.2 |
| v4.0 | 2026-09-04 | 新增 FR-23 合盖不休眠开关(看板+托盘,备份恢复,退出自动还原);坑:powercfg 输出 GBK,text=True 默认 UTF-8 解码炸成 None;软件版本 v0.5.3 |
